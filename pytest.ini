[pytest]
# `pythonpath = .` hace que `common`, `ingestion` y `transform` sean importables
# desde los tests sin instalar el proyecto ni tocar PYTHONPATH a mano.
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    needs_silver: requiere que las tablas Silver estén construidas
